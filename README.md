# Виджет банковских операций клиента

## Суть проекта:

IT-отделу крупного банка поручено добавить новую функциональность для личного кабинета клиента.
Нужно разработать виджет, который показывает несколько последних успешных банковских операций клиента.
Этот проект является реализацией бэкенд составляющей. По завершению разработки, программа будет готовить
данные для отображения в новом виджете на стороне фронтэнда.

### Технический стек:

Для проекта требуется версия python не ниже `3.11`  
А так же версия Poetry не ниже `2.1.4`

В проекте используются:

* Менеджер пакетов `Poetry`.
* Линтеры: `mypy`, `black`, `isort`, `flake8`
* Библиотека `datetime`
* Фреймворк управления тестами `pytest`
* Инструмент сбора статистики покрытия кода `pytest-cov`
* Библиотека отправки запросов к API `requests`
* Библиотека для работы с переменными окружения `python-dotenv`
* Библиотека для взаимодействия с операционной системой `os`
* Библиотека для взаимодействия с csv файлами `csv`
* Библиотека для взаимодействия с табличными структурами данных и файлами `pandas`
* Библиотека для чтения и записи файлов `Excel`
* Файлы с типами (type stubs) для `pandas`
* Библиотека для логирования событий работы системы `logging`
* Библиотека для работы с регулярными выражениями `re`
* Библиотека для работы с коллекциями `collections`

### Установка:

1. Склонируйте проект на свое локальное окружение командой:  
   git clone https://github.com/Uriy-T/python_study_skypro.git

   или

   git clone https://github.com/Uriy-T/python_study_skypro.git

2. Убедитесь, что в среде разработки для проекта используются интерпретатор python  
   и poetry версии в соответствии с указанными требованиями.

3. Для установки всех необходимых зависимостей выполните команду:
   ```bash
   poetry install

### Тесты и тестирование:

#### Аннотация 
Тесты расположены в пакете "tests" в корне проекта. Тесты параметризованы с помощью маркировки
`pytest.mark.parametrise()`. Тесты гораздо легче использовать благодаря тому, что в качестве значений параметров в
маркировку передаются не фиксированные значения, в результаты вычисления двух функций-форматеров `param_packer` и
`value_packer` в которые передается набор тестовых данных, приводимый ими к нужному для маркировки состоянию. Что
позволяет менять не сами тесты, а только сами наборы данных.
Наборы данных вынесены в отдельный пакет и разбиты тематически соответственно названию тестируемого модуля и функции.

#### Запуск тестов
Все тесты разом можно запустить командой:

```bash
  pytest
  ```
Если требуется запустить конкретные тесты отдельно, то используйте команды:

* Запуск конкретного теста:
```bash
  pytest -k 'Имя теста или его часть'
  ```
Часть имени теста должна достаточно четко указывать на этот самый тест. Чтобы в прогон не попали другие, обладающие сходным наименованием с общей частью в названии.

* Запуск тестов со сбором покрытия:

```bash
  pytest --cov=src --cov-report=html
  ```
Перед запуском команды для полной уверенности в актуальности данных отслеживания покрытия рекомендуется удалить из проекта пакет htmlcov и файл .coverage.

### Модульный состав:

Все основные функциональные модули расположены в пакете src:
* generators.py - содержит генераторы данных по картам и фильтры данных по операциям
* masks.py - содержит функции для маскИрования номеров карт и счетов клиента банка
* processing.py - содержит функции для фильтрации данных об операциях по заданным признакам.
* widget.py - содержит функцию-диспетчер для выбора алгоритма маскИрования реквизита клиента и функцию-форматер даты из ISO формата.
* decorators.py - содержит декоратор log, собирающий статистику о выполнении декорированных функций. Вывод логов доступен в форме вывода в консоль и во внешний файл.
* utils.py - содержит утилитарные функции. Например, get_transaction_info позволяющую читать данные из JSON файлов по указанному пути.
* external_api.py - содержит функции выполняющие свои задачи, в том числе обращаясь к внешним API.
* table_data_readers.py - содержит функции для чтения данных из внешних источников табличного типа. Такие, как csv и xlsx файлы.

### Переменные окружения:

В проекте есть файл `.env_example`. В нем вы можете найти шаблон перечисления важных для работы проекта переменных, содержащих чувствительные данные. Скачав проект, создайте на его основе локальный .env файл и заполните переменные значениями актуальными дял вашего локального окружения.

### Глобальные переменные:
В проекте есть файл `config.py`, в котором содержатся переменные, значения которых могут использоваться и оказывать влияние на работу функций в разных частях приложения. Создавая и изменяя их в этом файле, можно гибко и быстро вносить корректуры в возможности работы приложения.

### Запуск приложения:

В проекте есть файл `main.py`, содержащий бизнес логику приложения. Файл можно запустить из него стандартными средствами IDE. Или из терминала командой:

```bash
   python main.py
```
#### ВНИМАНИЕ!!!
Запуская приложение из терминала, убедитесь что находитесь в корневой директории проекта.